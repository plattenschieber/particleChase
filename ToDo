- check if there are already 5 particles inside quads particle array and flag quad to refine
- free all unneeded data 
- initialize quadData->nParticles in init_fn
- create point array with #particles given from function caller and make miniQuad an array
